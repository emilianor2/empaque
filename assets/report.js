// Por ahora liviano: podrías agregar persistencia de filtros en localStorage si querés.
document.getElementById('filterForm')?.addEventListener('submit', () => {
  // futuro: guardar filtros en localStorage
});
